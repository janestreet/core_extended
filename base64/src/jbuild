(library
 ((name base64)
  (public_name core_extended.base64)
  (flags (:standard -unsafe-string))
  (libraries
   (core_kernel))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))

(jbuild_version 1)
