(library
 ((name selector)
  (public_name core_extended.selection)
  (flags (:standard -safe-string))
  (libraries
   (re
    re.perl
    core_kernel))
  (preprocess (pps (ppx_jane ppxlib.runner)))
))


(jbuild_version 1)
