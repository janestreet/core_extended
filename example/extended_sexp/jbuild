(executables
 ((names (main))
  (libraries (async
                    core
                    core_extended
                    fieldslib
                    sexplib))))

(jbuild_version jane_street)
