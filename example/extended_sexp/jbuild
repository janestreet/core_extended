(executables
 ((names (main))
  (flags (:standard -safe-string))
  (libraries (async
              core
              core_extended
              fieldslib
              sexplib))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))

(jbuild_version 1)
