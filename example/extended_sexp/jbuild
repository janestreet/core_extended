(executables
 ((names (main))
  (libraries (async
                    core
                    core_extended
                    fieldslib
                    sexplib))
  (preprocess (pps (ppx_jane)))))

(jbuild_version jane_street)
