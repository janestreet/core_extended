(executables
 ((names (main))
  (libraries (async
              core
              core_extended
              fieldslib
              sexplib))
  (preprocess (pps (ppx_jane ppxlib.runner)))))


(jbuild_version 1)
