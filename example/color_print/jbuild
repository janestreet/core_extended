(executables
 ((names (test_colors))
  (flags (:standard -safe-string))
  (libraries (core
              core_extended
              ))
  (preprocess (pps (ppx_jane ppxlib.runner)))))


(jbuild_version 1)
