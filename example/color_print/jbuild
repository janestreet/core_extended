(executables
 ((names (test_colors))
  (flags (:standard -safe-string))
  (libraries (core
              core_extended
              ))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))

(jbuild_version 1)
